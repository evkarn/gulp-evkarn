# Prettier проверяет только код сборки и документацию.
#
# Исходники сайта (src/) исторически не форматировались: разовый прогон
# prettier по ним дал бы коммит на тысячи строк. Если захотите привести
# их в порядок — уберите src из этого файла и запустите npm run format:fix.
#
# Замечания по форматированию JS в src всё равно видны через
# npm run lint:js (правило prettier/prettier выводит предупреждения).

**/.git
**/.svn
**/.hg
**/node_modules
dist/

package-lock.json

src/

# Личные файлы редактора и заметки — не трогаем
.vscode/
agents.md

# У файла нет парсера — это шаблон переменных окружения
.env.example

gulp/version.json
