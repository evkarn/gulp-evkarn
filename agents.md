# Сборка доработана. Готовим к публикации в паблик.

## Что уже сделано (предыдущая сессия):

- Починены таски (js/html/styles/ftp/zip/ssh), plumber пишет ошибки
  в терминал без попапов.
- Креды FTP/SSH вынесены в .env (+ .env.example), в коде секретов нет.
- Деплой-сценарии пересобирают проект перед выгрузкой; favicon/files/config
  включены в основную сборку; шрифты конвертируются автоматически.
- imgMin переведён с merge-stream на последовательные потоки (терялись AVIF).
- Линтеры настроены и работают: JS проверяет только код сборки и
  scripts.js (архив src/js/functions намеренно не проверяется).
  Prettier проверяет только код сборки (src исторический).
- README.md переписан полностью.

## Что делаем дальше:

### Публикация

- [ ] Добавить LICENSE (MIT) — в package.json указан ISC, файла нет.
- [ ] Добавить engines в package.json: "node": ">=20".
- [ ] Заполнить repository при выкладке на GitHub.
- [ ] Упомянуть в README готовые сниппеты VS Code из папки .vscode/.
- [ ] Помнить: в git-истории остался старый FTP-пароль (не актуален,
      владелец в курсе).

### README: раздел «Стили» расширить разделом «Инструменты»

- [ ] Каталог миксинов из styles/scss/mixins/: adaptive-value, resp-val,
      fluid-size, breakpoints-px/em, absolute-center, overlay, states
      (state-link, state-accent, state-buttons и др.) — для каждого:
      сигнатура, пример вызова, что выдаёт.
- [ ] Конвенции: BEM (stylelint проверяет через bem-pattern), порядок
      css-свойств (stylelint-order), структура vars/ и settings/.

### README: раздел «JS» расширить каталогом сниппетов

- [ ] Оглавление src/js/functions/ (~50 решений) по группам: UI-компоненты,
      прокрутка, формы, модалки, слайдеры, утилиты — однострочное описание.
- [ ] Пример подключения через алиасы (@funcs, @utils и т.д.).
- [ ] Описать паттерн хранения: func.js + демо-html + стили рядом —
      блок копируется целиком.
- [ ] Отдельно описать modules/, utils/, constants/.

## Как работаем:

- Смотришь файл
- Рассказываешь, что будешь делать и ждёшь разрешения
- Как только я разрешаю, оформляем файл.
